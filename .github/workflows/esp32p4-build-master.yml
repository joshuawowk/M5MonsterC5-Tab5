name: ESP32P4 Build (IDF 5.4.1)

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Optional tag name to upload assets to a release (example 'v1.2.3')"
        required: false
  push:
    branches: [main, master]
    paths:
      - "main/main.c"
      - ".github/scripts/**"
      - ".github/workflows/esp32p4-build-master.yml"
  release:
    types: [published, edited]

jobs:
  changes:
    name: Detect workflow changes
    runs-on: ubuntu-24.04
    outputs:
      workflow_changed: ${{ steps.filter.outputs.workflow_changed }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check workflow diff
        id: filter
        run: |
          set -euo pipefail
          if [ "${GITHUB_EVENT_NAME:-}" != "push" ]; then
            echo "workflow_changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          base="${{ github.event.before }}"
          head="${{ github.sha }}"
          if [ -z "$base" ] || [ "$base" = "0000000000000000000000000000000000000000" ]; then
            base="$(git rev-parse HEAD~1 2>/dev/null || git rev-parse HEAD)"
          fi

          if git diff --name-only "$base" "$head" | grep -qE '^\.github/workflows/'; then
            echo "workflow_changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "workflow_changed=false" >> "$GITHUB_OUTPUT"
          fi

  firmware:
    name: M5MonsterC5-Tab5
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    container:
      image: espressif/idf:v5.4.1
    outputs:
      fw_version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache ESP-IDF artifacts
        uses: actions/cache@v4
        with:
          path: /root/.espressif
          key: espidf-5.4.1-${{ runner.os }}-${{ hashFiles('sdkconfig', 'dependencies.lock') }}
          restore-keys: |
            espidf-5.4.1-${{ runner.os }}-
            espidf-5.4.1-

      - name: Build firmware (IDF 5.4.1)
        run: bash .github/scripts/container_build.sh --no-docker

      - name: Stamp firmware with JANOS_TAB_VERSION
        id: version
        run: |
          set -e
          version=$(python - <<'PY'
          from pathlib import Path
          import re
          text = Path("main/main.c").read_text()
          m = re.search(r'JANOS_TAB_VERSION\s*"([^"]+)"', text)
          if not m:
              raise SystemExit("JANOS_TAB_VERSION not found")
          print(m.group(1))
          PY
          )
          src="binaries-esp32p4/M5MonsterC5-Tab5.bin"
          dst="binaries-esp32p4/M5MonsterC5-Tab5-${version}.bin"
          if [ ! -f "$src" ]; then
            echo "Missing firmware: $src" >&2
            exit 1
          fi
          cp "$src" "$dst"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "release_asset=${dst}" >> "$GITHUB_OUTPUT"

      - name: Merge full flash image (ESP32P4)
        shell: bash
        run: |
          set -euo pipefail
          if ! python3 -c "import venv" >/dev/null 2>&1; then
            apt-get update
            apt-get install -y python3-venv
          fi
          python3 -m venv /tmp/esptool-venv
          /tmp/esptool-venv/bin/python -m pip install --no-cache-dir esptool
          /tmp/esptool-venv/bin/python -m esptool --chip esp32p4 merge_bin \
            -o "binaries-esp32p4/M5MonsterC5-Tab5-full-${{ steps.version.outputs.version }}.bin" \
            --flash_mode dio --flash_freq 80m --flash_size 16MB \
            0x2000 "binaries-esp32p4/bootloader.bin" \
            0x8000 "binaries-esp32p4/partition-table.bin" \
            0x10000 "binaries-esp32p4/M5MonsterC5-Tab5.bin"
          cp "binaries-esp32p4/M5MonsterC5-Tab5-full-${{ steps.version.outputs.version }}.bin" \
            "binaries-esp32p4/M5MonsterC5-Tab5-full.bin"

      - name: Package for M5StackLauncher
        id: launcher
        shell: bash
        run: |
          set -euo pipefail
          version="${{ steps.version.outputs.version }}"
          python3 tools/package_launcher.py \
            --build-dir binaries-esp32p4 \
            --out-dir binaries-esp32p4 \
            --name "M5MonsterC5-Tab5-launcher.bin" \
            --version "$version"
          cp binaries-esp32p4/M5MonsterC5-Tab5-launcher.bin \
            "binaries-esp32p4/M5MonsterC5-Tab5-launcher-${version}.bin"

      - name: Package firmware zip
        id: bundle
        run: |
          set -e
          cd binaries-esp32p4
          out="M5MonsterC5-Tab5-${{ steps.version.outputs.version }}.zip"
          rm -f "$out"
          zip -9 "$out" \
            bootloader.bin \
            partition-table.bin \
            flash_board.py \
            launcher-manifest.json \
            M5MonsterC5-Tab5-launcher.bin \
            "M5MonsterC5-Tab5-launcher-${{ steps.version.outputs.version }}.bin" \
            M5MonsterC5-Tab5-full.bin \
            "M5MonsterC5-Tab5-full-${{ steps.version.outputs.version }}.bin" \
            "M5MonsterC5-Tab5-${{ steps.version.outputs.version }}.bin" \
            M5MonsterC5-Tab5.bin
          echo "bundle_zip=$(pwd)/$out" >> "$GITHUB_OUTPUT"

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: esp32p4-firmware
          path: binaries-esp32p4
          if-no-files-found: error

  release:
    name: Release
    needs: [changes, firmware]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      pull-requests: read
    if: (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main' || github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.release_tag != '')) && needs.changes.outputs.workflow_changed != 'true'
    steps:
      - name: Download firmware artifacts
        uses: actions/download-artifact@v4
        with:
          name: esp32p4-firmware
          path: release_artifacts/firmware

      - name: Package full bundle
        run: |
          set -e
          cd release_artifacts
          mkdir -p bundle
          FW_VERSION="${{ needs.firmware.outputs.fw_version }}"

          shopt -s nullglob

          files=(
            firmware/bootloader.bin
            firmware/partition-table.bin
            firmware/flash_board.py
            firmware/M5MonsterC5-Tab5-full.bin
            "firmware/M5MonsterC5-Tab5-full-${FW_VERSION}.bin"
            firmware/M5MonsterC5-Tab5.bin
            "firmware/M5MonsterC5-Tab5-${FW_VERSION}.bin"
            firmware/M5MonsterC5-Tab5-launcher.bin
            "firmware/M5MonsterC5-Tab5-launcher-${FW_VERSION}.bin"
            firmware/launcher-manifest.json
          )

          if [ -f "firmware/M5MonsterC5-Tab5-${FW_VERSION}.zip" ]; then
            files+=("firmware/M5MonsterC5-Tab5-${FW_VERSION}.zip")
          fi

          out="bundle/M5MonsterC5-Tab5-${FW_VERSION}-bundle.zip"
          zip -9 "$out" "${files[@]}"

      - name: Build release notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FW_VERSION: ${{ needs.firmware.outputs.fw_version }}
          REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ github.event.release.tag_name || inputs.release_tag || format('v{0}', needs.firmware.outputs.fw_version) }}
        run: |
          set -euo pipefail
          notes_file="release_notes.md"
          TAG="${RELEASE_TAG:-$FW_VERSION}"
          BASE_URL="https://github.com/$REPO/releases/download/$TAG"
          FW_ZIP="$BASE_URL/M5MonsterC5-Tab5-${FW_VERSION}.zip"
          BUNDLE_ZIP="$BASE_URL/M5MonsterC5-Tab5-${FW_VERSION}-bundle.zip"
          RELEASE_PAGE="https://github.com/$REPO/releases/tag/$TAG"
          PAGES_LINK="https://${REPO%/*}.github.io/${REPO#*/}/"

          {
            printf '%s\n\n' "Release ${TAG} published."
            printf '%s\n' "Release page: ${RELEASE_PAGE}"
            printf '%s\n' "Web flasher: ${PAGES_LINK}"
            printf '%s\n' "Firmware (${FW_VERSION}): ${FW_ZIP}"
            printf '%s\n\n' "Bundle: ${BUNDLE_ZIP}"
          } > "$notes_file"

          if [ "${GITHUB_EVENT_NAME:-}" = "release" ]; then
            if [ -n "${GITHUB_EVENT_PATH:-}" ] && [ -f "${GITHUB_EVENT_PATH:-}" ]; then
              release_body="$(jq -r '.release.body // ""' "$GITHUB_EVENT_PATH")"
              if [ -n "$(printf '%s' "$release_body" | tr -d '\r\n[:space:]')" ]; then
                printf '%s\n' "$release_body" >> "$notes_file"
              fi
            fi
            exit 0
          fi

          if [ "${GITHUB_EVENT_NAME:-}" = "push" ] && [ -n "${GITHUB_EVENT_PATH:-}" ] && [ -f "${GITHUB_EVENT_PATH:-}" ]; then
            merge_message="$(jq -r '.head_commit.message // ""' "$GITHUB_EVENT_PATH")"
            merge_title="$(printf '%s' "$merge_message" | sed -n '1p')"
            merge_body="$(printf '%s\n' "$merge_message" | tail -n +2 | sed '/./,$!d')"
            if [ -n "$(printf '%s' "$merge_body" | tr -d '\r\n[:space:]')" ]; then
              if [ -n "$(printf '%s' "$merge_title" | tr -d '\r\n[:space:]')" ]; then
                printf '%s\n\n' "$merge_title" >> "$notes_file"
              fi
              printf '%s\n' "$merge_body" >> "$notes_file"
              exit 0
            fi
          fi

          if command -v gh >/dev/null 2>&1; then
            api="/repos/${GITHUB_REPOSITORY}/commits/${GITHUB_SHA}/pulls"
            pr_json="$(gh api -H "Accept: application/vnd.github+json" "$api" 2>/dev/null || echo '[]')"
            pr_title="$(printf '%s' "$pr_json" | jq -r '.[0].title // ""')"
            pr_body="$(printf '%s' "$pr_json" | jq -r '.[0].body // ""')"

            if [ -n "$(printf '%s' "$pr_body" | tr -d '\r\n[:space:]')" ]; then
              if [ -n "$pr_title" ]; then
                printf '%s\n\n' "$pr_title" >> "$notes_file"
              fi
              printf '%s\n' "$pr_body" >> "$notes_file"
              exit 0
            fi

            gen_json="$(gh api -X POST -H "Accept: application/vnd.github+json" \
              "/repos/${GITHUB_REPOSITORY}/releases/generate-notes" \
              -f tag_name="$TAG" -f target_commitish="$GITHUB_SHA" 2>/dev/null || echo '')"
            gen_body="$(printf '%s' "$gen_json" | jq -r '.body // ""')"
            if [ -n "$(printf '%s' "$gen_body" | tr -d '\r\n[:space:]')" ]; then
              printf '%s\n' "$gen_body" >> "$notes_file"
            fi
          fi

      - name: Upload binaries to GitHub Release (notes)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name || inputs.release_tag || format('v{0}', needs.firmware.outputs.fw_version) }}
          name: ${{ format('M5MonsterC5-Tab5 {0}', needs.firmware.outputs.fw_version) }}
          body_path: release_notes.md
          files: |
            release_artifacts/firmware/bootloader.bin
            release_artifacts/firmware/partition-table.bin
            release_artifacts/firmware/flash_board.py
            release_artifacts/firmware/M5MonsterC5-Tab5-full.bin
            release_artifacts/firmware/M5MonsterC5-Tab5-full-${{ needs.firmware.outputs.fw_version }}.bin
            release_artifacts/firmware/M5MonsterC5-Tab5.bin
            release_artifacts/firmware/M5MonsterC5-Tab5-${{ needs.firmware.outputs.fw_version }}.bin
            release_artifacts/firmware/M5MonsterC5-Tab5-${{ needs.firmware.outputs.fw_version }}.zip
            release_artifacts/bundle/M5MonsterC5-Tab5-${{ needs.firmware.outputs.fw_version }}-bundle.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  pages:
    name: Pages Web Flasher
    needs: [release, firmware]
    if: ${{ needs.release.result == 'success' }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    outputs:
      page_url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          enablement: true

      - name: Resolve release tag
        id: meta
        env:
          EVENT_TAG: ${{ github.event.release.tag_name }}
          INPUT_TAG: ${{ inputs.release_tag }}
          FW_VERSION: ${{ needs.firmware.outputs.fw_version }}
        run: |
          set -euo pipefail
          TAG="${EVENT_TAG:-}"
          if [ -z "$TAG" ]; then TAG="${INPUT_TAG:-}"; fi
          if [ -z "$TAG" ]; then TAG="v${FW_VERSION}"; fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Using tag: $TAG"

      - name: Download release assets
        id: download
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ steps.meta.outputs.tag }}"
          rm -rf docs/firmware
          mkdir -p docs/firmware

          gh release download "$TAG" -R "$GITHUB_REPOSITORY" --dir docs/firmware \
            --pattern "bootloader.bin" \
            --pattern "partition-table.bin" \
            --pattern "M5MonsterC5-Tab5*.bin"

          ls -lh docs/firmware

          if [ ! -f docs/firmware/M5MonsterC5-Tab5.bin ]; then
            alt=$(ls docs/firmware/M5MonsterC5-Tab5-*.bin 2>/dev/null | head -n1 || true)
            if [ -n "$alt" ]; then
              cp "$alt" docs/firmware/M5MonsterC5-Tab5.bin
            fi
          fi

          for f in bootloader.bin partition-table.bin M5MonsterC5-Tab5.bin; do
            if [ ! -f "docs/firmware/$f" ]; then
              echo "Missing $f in release assets for tag ${TAG}" >&2
              exit 1
            fi
          done

          fw_version=$(ls docs/firmware/M5MonsterC5-Tab5-*.bin 2>/dev/null | head -n1 | xargs -n1 basename | sed -E 's/^M5MonsterC5-Tab5-([^.]*)\\.bin$/\\1/')
          if [ -z "$fw_version" ]; then
            fw_version="${TAG}"
          fi
          echo "fw_version=$fw_version" >> "$GITHUB_OUTPUT"

      - name: Build manifest.json
        env:
          FW_VERSION: ${{ steps.download.outputs.fw_version }}
          BUILD_TAG: ${{ steps.meta.outputs.tag }}
        run: |
          set -euo pipefail
          python -c "import os, json; from pathlib import Path; version=os.getenv('FW_VERSION') or os.getenv('BUILD_TAG'); build=os.getenv('BUILD_TAG','manual'); manifest={'name':'M5MonsterC5-Tab5 ESP32-P4','version':version,'build':build,'chipFamily':'ESP32-P4','parts':[{'path':'firmware/bootloader.bin','offset':8192},{'path':'firmware/partition-table.bin','offset':32768},{'path':'firmware/M5MonsterC5-Tab5.bin','offset':65536}]}; Path('docs/manifest.json').write_text(json.dumps(manifest, indent=2))"

      - name: Delete stale github-pages artifact (if re-run)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          artifact_id=$(gh api \
            "/repos/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID/artifacts" \
            --jq '.artifacts[] | select(.name=="github-pages") | .id' 2>/dev/null || true)
          for id in $artifact_id; do
            echo "Deleting stale artifact id=$id"
            gh api -X DELETE "/repos/$GITHUB_REPOSITORY/actions/artifacts/$id" || true
          done

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  discord:
    name: Discord
    needs: [release, firmware, pages]
    runs-on: ubuntu-24.04
    steps:
      - name: Notify Discord
        if: success()
        env:
          DISCORD_WEBHOOK: ${{ secrets.GIT_BUILD }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FW_VERSION: ${{ needs.firmware.outputs.fw_version }}
          RELEASE_TAG: ${{ github.event.release.tag_name || inputs.release_tag || format('v{0}', needs.firmware.outputs.fw_version) }}
          REPO: ${{ github.repository }}
          PAGES_URL: ${{ needs.pages.outputs.page_url || format('https://{0}.github.io/{1}/', github.repository_owner, github.event.repository.name) }}
        run: |
          set -euo pipefail
          if [ -z "$DISCORD_WEBHOOK" ]; then
            echo "DISCORD_WEBHOOK not configured, skipping notification"
            exit 0
          fi

          TAG="${RELEASE_TAG:-$FW_VERSION}"
          BASE_URL="https://github.com/$REPO/releases/download/$TAG"
          FW_ZIP="$BASE_URL/M5MonsterC5-Tab5-${FW_VERSION}.zip"
          BUNDLE_ZIP="$BASE_URL/M5MonsterC5-Tab5-${FW_VERSION}-bundle.zip"
          RELEASE_PAGE="https://github.com/$REPO/releases/tag/$TAG"
          PAGES_LINK="${PAGES_URL:-https://${GITHUB_REPOSITORY%/*}.github.io/${GITHUB_REPOSITORY#*/}/}"

          RELEASE_BODY=""
          if [ "${GITHUB_EVENT_NAME:-}" = "release" ] && [ -n "${GITHUB_EVENT_PATH:-}" ] && [ -f "${GITHUB_EVENT_PATH:-}" ]; then
            RELEASE_BODY="$(jq -r '.release.body // ""' "$GITHUB_EVENT_PATH")"
          fi
          if [ -z "$(printf '%s' "$RELEASE_BODY" | tr -d '\r\n[:space:]')" ] && [ "${GITHUB_EVENT_NAME:-}" = "push" ] && [ -n "${GITHUB_EVENT_PATH:-}" ] && [ -f "${GITHUB_EVENT_PATH:-}" ]; then
            MERGE_MESSAGE="$(jq -r '.head_commit.message // ""' "$GITHUB_EVENT_PATH")"
            MERGE_TITLE="$(printf '%s' "$MERGE_MESSAGE" | sed -n '1p')"
            MERGE_BODY="$(printf '%s\n' "$MERGE_MESSAGE" | tail -n +2 | sed '/./,$!d')"
            if [ -n "$(printf '%s' "$MERGE_BODY" | tr -d '\r\n[:space:]')" ]; then
              if [ -n "$(printf '%s' "$MERGE_TITLE" | tr -d '\r\n[:space:]')" ]; then
                RELEASE_BODY="$(printf '%s\n\n%s' "$MERGE_TITLE" "$MERGE_BODY")"
              else
                RELEASE_BODY="$MERGE_BODY"
              fi
            fi
          fi
          if [ -z "$(printf '%s' "$RELEASE_BODY" | tr -d '\r\n[:space:]')" ]; then
            RELEASE_BODY="$(gh release view "$TAG" -R "$REPO" --json body --jq .body 2>/dev/null || true)"
          fi

          base_content=$(
            printf '%s\n' \
              "M5MonsterC5-Tab5" \
              "Release ${TAG} published." \
              "Release page:" \
              "${RELEASE_PAGE}" \
              "Web flasher:" \
              "${PAGES_LINK}" \
              "Firmware (${FW_VERSION}):" \
              "${FW_ZIP}" \
              "Bundle:" \
              "${BUNDLE_ZIP}"
          )

          RELEASE_BODY="$(printf '%s' "$RELEASE_BODY" | tr -d '\r')"
          base_len=$(printf '%s' "$base_content" | wc -c | tr -d ' ')
          room=$((1990 - base_len))
          if [ "$room" -gt 0 ] && [ -n "$(printf '%s' "$RELEASE_BODY" | tr -d '\n[:space:]')" ]; then
            body_trimmed="$(printf '%s' "$RELEASE_BODY" | head -c "$room")"
            content="$(printf '%s\n%s' "$base_content" "$body_trimmed")"
          else
            content="$base_content"
          fi

          payload=$(jq -n --arg content "$content" '{content:$content}')
          curl -X POST -H "Content-Type: application/json" -d "$payload" "$DISCORD_WEBHOOK"
