# Phase 4.4 execution

Scope approved by the user's request to proceed: original nmap, IoT, GITM and
wpa-sec screens with deterministic offline results. Edit the current working
tree. Full compilation and browser runs are delegated to the user.

1. Shared simulated operation lifecycle: busy/connection guards, cancellation,
   stable synthetic results, module isolation and GITM virtual capture storage.
2. Native nmap and IoT views, replacing UART/task boundaries.
3. Native GITM setup/live/stop/copy and exit flow.
4. Native wpa-sec credentials and simulated upload result flow.
5. Integrate explicit slice decisions, cooperative ticks and binding identities.
6. Run inexpensive model/syntax checks; supply browser tests, full gate and handoff.

Ruling: 4.3 remains awaiting user build/acceptance; proceeding with 4.4 does not
claim that the earlier gate passed. Retain original LVGL layout wherever possible.
Passwords are not stored in JS model jobs, fixtures or test reports.

Progress: implementation and inexpensive checks complete; full build and browser
acceptance remain with the user. See Phase_4_4_Report.md for commands and evidence.
Review fixes: per-container binding ownership, distinct menu/field identities,
native scan-level counts and compatible GITM basename validation.
